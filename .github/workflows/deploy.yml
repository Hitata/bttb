name: Build & Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bun x prisma generate

      - name: Lint
        run: bun run lint

      - name: Build
        run: bun run build
        env:
          DATABASE_URL: "file:./dev.db"
          AUTH_SECRET: "ci-build-secret-not-for-production"

  # Uncomment and configure for your deployment target:
  #
  # deploy-docker:
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Build & push Docker image
  #       run: |
  #         docker build -t your-registry/bbtb-app .
  #         docker push your-registry/bbtb-app
  #
  # deploy-vercel:
  #   Note: If using Vercel, connect your repo at vercel.com
  #   No GitHub Action needed - Vercel deploys automatically on push.
