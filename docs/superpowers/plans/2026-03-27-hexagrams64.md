# Hexagrams 64 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert `hexagrams64.html` into a Next.js page at `/hexagrams` with a "Học" → "64 Quẻ" dropdown in the topbar using shadcn NavigationMenu.

**Architecture:** Extract static data (TRIGRAMS, HEXAGRAMS, ICHING_DATA) to `src/lib/hexagrams-data.ts`, build a client-side React page with grid (8×8 matrix) and circle (SVG) views, and replace the topbar `<nav>` with shadcn NavigationMenu that includes the new "Học" dropdown.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, shadcn/ui (base-nova style), lucide-react

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/lib/hexagrams-data.ts` | Static data: TRIGRAMS, HEXAGRAMS, ICHING_DATA, helper functions |
| Install | `src/components/ui/navigation-menu.tsx` | shadcn NavigationMenu component |
| Create | `src/components/app-nav.tsx` | NavigationMenu with "Học" dropdown |
| Modify | `src/app/layout.tsx` | Replace `<nav>` with `<AppNav />` |
| Create | `src/app/hexagrams/page.tsx` | 64 Quẻ reference page (client component) |

---

## Task 1: Create hexagrams data file

**Files:**
- Create: `src/lib/hexagrams-data.ts`

- [ ] **Step 1: Create the file with types, TRIGRAMS, helpers**

```ts
// src/lib/hexagrams-data.ts

export interface Trigram {
  cn: string
  vn: string
  pinyin: string
  nature: string
  en: string
  el: 'metal' | 'fire' | 'wood' | 'water' | 'earth'
  lines: [number, number, number] // 1=yang, 0=yin, indices 0=top, 1=mid, 2=bottom
  family: string
}

export interface Hexagram {
  num: number
  cn: string
  pinyin: string
  vn: string
  vnFull: string
  en: string
  upperTriIdx: number
  lowerTriIdx: number
  description: string
}

export interface IChingEntry {
  judgment: { vn: string; en: string }
  image: { vn: string; en: string }
  lines: { vn: string; en: string }[]
}

export const ELEMENT_COLORS: Record<string, string> = {
  fire: '#e04a2f',
  water: '#2a7de1',
  wood: '#3aaa5b',
  metal: '#c9c0a8',
  earth: '#c4943a',
}

export const TRIGRAMS: Trigram[] = [
  { cn: '乾', vn: 'Càn',  pinyin: 'Qián', nature: 'Thiên 天', en: 'Heaven',   el: 'metal', lines: [1,1,1], family: 'Cha' },
  { cn: '兌', vn: 'Đoài', pinyin: 'Duì',  nature: 'Trạch 澤', en: 'Lake',     el: 'metal', lines: [0,1,1], family: 'Thiếu nữ' },
  { cn: '離', vn: 'Ly',   pinyin: 'Lí',   nature: 'Hỏa 火',   en: 'Fire',     el: 'fire',  lines: [1,0,1], family: 'Trung nữ' },
  { cn: '震', vn: 'Chấn', pinyin: 'Zhèn', nature: 'Lôi 雷',   en: 'Thunder',  el: 'wood',  lines: [0,0,1], family: 'Trưởng nam' },
  { cn: '巽', vn: 'Tốn',  pinyin: 'Xùn',  nature: 'Phong 風', en: 'Wind',     el: 'wood',  lines: [1,1,0], family: 'Trưởng nữ' },
  { cn: '坎', vn: 'Khảm', pinyin: 'Kǎn',  nature: 'Thủy 水',  en: 'Water',    el: 'water', lines: [0,1,0], family: 'Trung nam' },
  { cn: '艮', vn: 'Cấn',  pinyin: 'Gèn',  nature: 'Sơn 山',   en: 'Mountain', el: 'earth', lines: [1,0,0], family: 'Thiếu nam' },
  { cn: '坤', vn: 'Khôn', pinyin: 'Kūn',  nature: 'Địa 地',   en: 'Earth',    el: 'earth', lines: [0,0,0], family: 'Mẹ' },
]
```

- [ ] **Step 2: Add HEXAGRAMS array**

Open `hexagrams64.html` at line 749. The array format is:
`[num, cn, pinyin, vn, vnFull, en, upperTriIdx, lowerTriIdx, description]`

Convert each entry to the `Hexagram` interface. Append to `hexagrams-data.ts`:

```ts
export const HEXAGRAMS: Hexagram[] = [
  { num: 1,  cn: '乾', pinyin: 'qián',     vn: 'Càn',       vnFull: 'Thuần Càn',            en: 'The Creative',           upperTriIdx: 0, lowerTriIdx: 0, description: 'Thuần dương cương kiện. Nguyên · Hanh · Lợi · Trinh. Sức mạnh sáng tạo nguyên thủy.' },
  { num: 2,  cn: '坤', pinyin: 'kūn',      vn: 'Khôn',      vnFull: 'Thuần Khôn',           en: 'The Receptive',          upperTriIdx: 7, lowerTriIdx: 7, description: 'Thuần âm nhu thuận. Đất mẹ sinh sôi, nâng đỡ vạn vật.' },
  { num: 3,  cn: '屯', pinyin: 'zhūn',     vn: 'Truân',     vnFull: 'Thủy Lôi Truân',       en: 'Difficulty at Beginning', upperTriIdx: 5, lowerTriIdx: 3, description: 'Khởi đầu gian truân. Vạn vật mới sinh, đầy thử thách.' },
  { num: 4,  cn: '蒙', pinyin: 'méng',     vn: 'Mông',      vnFull: 'Sơn Thủy Mông',        en: 'Youthful Folly',         upperTriIdx: 6, lowerTriIdx: 5, description: 'U mê cần khai sáng. Như trẻ thơ cần dẫn dắt.' },
  { num: 5,  cn: '需', pinyin: 'xū',       vn: 'Nhu',       vnFull: 'Thủy Thiên Nhu',       en: 'Waiting',                upperTriIdx: 5, lowerTriIdx: 0, description: 'Chờ đợi đúng thời. Kiên nhẫn trước hiểm nguy.' },
  { num: 6,  cn: '訟', pinyin: 'sòng',     vn: 'Tụng',      vnFull: 'Thiên Thủy Tụng',      en: 'Conflict',               upperTriIdx: 0, lowerTriIdx: 5, description: 'Tranh tụng, kiện cáo. Nội bộ bất hòa với hoàn cảnh.' },
  { num: 7,  cn: '師', pinyin: 'shī',      vn: 'Sư',        vnFull: 'Địa Thủy Sư',          en: 'The Army',               upperTriIdx: 7, lowerTriIdx: 5, description: 'Quân đội, kỷ luật. Cần tổ chức và lãnh đạo chính nghĩa.' },
  { num: 8,  cn: '比', pinyin: 'bǐ',       vn: 'Tỷ',        vnFull: 'Thủy Địa Tỷ',          en: 'Holding Together',       upperTriIdx: 5, lowerTriIdx: 7, description: 'Thân cận, liên kết. Nước trên đất, mối quan hệ gắn bó.' },
  { num: 9,  cn: '小畜', pinyin: 'xiǎo xù', vn: 'Tiểu Súc', vnFull: 'Phong Thiên Tiểu Súc', en: 'Small Taming',           upperTriIdx: 4, lowerTriIdx: 0, description: 'Chứa nhỏ, kiềm chế nhẹ. Tích lũy từng bước.' },
  { num: 10, cn: '履', pinyin: 'lǚ',       vn: 'Lý',        vnFull: 'Thiên Trạch Lý',       en: 'Treading',               upperTriIdx: 0, lowerTriIdx: 1, description: 'Dẫm lên, cẩn trọng. Đạp lên đuôi hổ mà không bị cắn.' },
  { num: 11, cn: '泰', pinyin: 'tài',      vn: 'Thái',      vnFull: 'Địa Thiên Thái',       en: 'Peace',                  upperTriIdx: 7, lowerTriIdx: 0, description: 'Thái bình, hanh thông. Trời đất giao hòa.' },
  { num: 12, cn: '否', pinyin: 'pǐ',       vn: 'Bĩ',        vnFull: 'Thiên Địa Bĩ',         en: 'Standstill',             upperTriIdx: 0, lowerTriIdx: 7, description: 'Bế tắc, trì trệ. Trời đất bất giao.' },
  { num: 13, cn: '同人', pinyin: 'tóng rén', vn: 'Đồng Nhân', vnFull: 'Thiên Hỏa Đồng Nhân', en: 'Fellowship',            upperTriIdx: 0, lowerTriIdx: 2, description: 'Đồng lòng, hòa đồng. Người cùng chí hướng.' },
  { num: 14, cn: '大有', pinyin: 'dà yǒu',  vn: 'Đại Hữu',  vnFull: 'Hỏa Thiên Đại Hữu',   en: 'Great Possession',       upperTriIdx: 2, lowerTriIdx: 0, description: 'Giàu lớn, phồn thịnh. Lửa trên trời, chiếu sáng muôn nơi.' },
  { num: 15, cn: '謙', pinyin: 'qiān',     vn: 'Khiêm',     vnFull: 'Địa Sơn Khiêm',        en: 'Modesty',                upperTriIdx: 7, lowerTriIdx: 6, description: 'Khiêm tốn, nhún nhường. Núi nằm dưới đất.' },
  { num: 16, cn: '豫', pinyin: 'yù',       vn: 'Dự',        vnFull: 'Lôi Địa Dự',           en: 'Enthusiasm',             upperTriIdx: 3, lowerTriIdx: 7, description: 'Vui vẻ, hoan hỉ. Sấm vang trên đất, muôn vật hưởng ứng.' },
  { num: 17, cn: '隨', pinyin: 'suí',      vn: 'Tùy',       vnFull: 'Trạch Lôi Tùy',        en: 'Following',              upperTriIdx: 1, lowerTriIdx: 3, description: 'Theo, thuận theo. Biết theo thời mà hành.' },
  { num: 18, cn: '蠱', pinyin: 'gǔ',       vn: 'Cổ',        vnFull: 'Sơn Phong Cổ',         en: 'Decay',                  upperTriIdx: 6, lowerTriIdx: 4, description: 'Hủ hoại, sửa chữa. Việc đã hỏng cần chỉnh đốn.' },
  { num: 19, cn: '臨', pinyin: 'lín',      vn: 'Lâm',       vnFull: 'Địa Trạch Lâm',        en: 'Approach',               upperTriIdx: 7, lowerTriIdx: 1, description: 'Đến gần, cai quản. Quyền lực đang lớn mạnh.' },
  { num: 20, cn: '觀', pinyin: 'guān',     vn: 'Quán',      vnFull: 'Phong Địa Quán',        en: 'Contemplation',          upperTriIdx: 4, lowerTriIdx: 7, description: 'Quan sát, chiêm nghiệm. Gió thổi trên đất, phóng tầm nhìn xa.' },
  { num: 21, cn: '噬嗑', pinyin: 'shì kè',  vn: 'Phệ Hạp',  vnFull: 'Hỏa Lôi Phệ Hạp',     en: 'Biting Through',         upperTriIdx: 2, lowerTriIdx: 3, description: 'Cắn đứt chướng ngại. Thực thi luật pháp.' },
  { num: 22, cn: '賁', pinyin: 'bì',       vn: 'Bí',        vnFull: 'Sơn Hỏa Bí',           en: 'Grace',                  upperTriIdx: 6, lowerTriIdx: 2, description: 'Trang sức, vẻ đẹp. Văn hoa bên ngoài.' },
  { num: 23, cn: '剝', pinyin: 'bō',       vn: 'Bác',       vnFull: 'Sơn Địa Bác',          en: 'Splitting Apart',        upperTriIdx: 6, lowerTriIdx: 7, description: 'Bóc lột, sụp đổ. Âm thịnh dương suy.' },
  { num: 24, cn: '復', pinyin: 'fù',       vn: 'Phục',      vnFull: 'Địa Lôi Phục',         en: 'Return',                 upperTriIdx: 7, lowerTriIdx: 3, description: 'Quay lại, phục hồi. Dương bắt đầu sinh trở lại.' },
  { num: 25, cn: '無妄', pinyin: 'wú wàng', vn: 'Vô Vọng',  vnFull: 'Thiên Lôi Vô Vọng',    en: 'Innocence',              upperTriIdx: 0, lowerTriIdx: 3, description: 'Không vọng tưởng. Chân thật, tự nhiên.' },
  { num: 26, cn: '大畜', pinyin: 'dà xù',   vn: 'Đại Súc',  vnFull: 'Sơn Thiên Đại Súc',    en: 'Great Taming',           upperTriIdx: 6, lowerTriIdx: 0, description: 'Chứa lớn, tích lũy lớn. Núi chứa trời, tiềm năng dồi dào.' },
  { num: 27, cn: '頤', pinyin: 'yí',       vn: 'Di',        vnFull: 'Sơn Lôi Di',           en: 'Nourishment',            upperTriIdx: 6, lowerTriIdx: 3, description: 'Nuôi dưỡng, di dưỡng. Chọn lựa lương thực cho tâm hồn.' },
  { num: 28, cn: '大過', pinyin: 'dà guò',  vn: 'Đại Quá',  vnFull: 'Trạch Phong Đại Quá',  en: 'Great Excess',           upperTriIdx: 1, lowerTriIdx: 4, description: 'Quá lớn, quá tải. Đòn nóc oằn xuống.' },
  { num: 29, cn: '坎', pinyin: 'kǎn',      vn: 'Khảm',      vnFull: 'Thuần Khảm',            en: 'The Abysmal',            upperTriIdx: 5, lowerTriIdx: 5, description: 'Hiểm trùng hiểm. Nước chồng nước, rơi vào vực sâu.' },
  { num: 30, cn: '離', pinyin: 'lí',       vn: 'Ly',        vnFull: 'Thuần Ly',              en: 'The Clinging',           upperTriIdx: 2, lowerTriIdx: 2, description: 'Sáng chồng sáng. Lửa dựa vào nhiên liệu mà tỏa sáng.' },
  { num: 31, cn: '咸', pinyin: 'xián',     vn: 'Hàm',       vnFull: 'Trạch Sơn Hàm',        en: 'Influence',              upperTriIdx: 1, lowerTriIdx: 6, description: 'Cảm ứng, giao cảm. Trẻ trai gái cảm nhau.' },
  { num: 32, cn: '恆', pinyin: 'héng',     vn: 'Hằng',      vnFull: 'Lôi Phong Hằng',       en: 'Duration',               upperTriIdx: 3, lowerTriIdx: 4, description: 'Bền lâu, kiên trì. Sấm và gió, bền bỉ không ngừng.' },
  { num: 33, cn: '遯', pinyin: 'dùn',      vn: 'Độn',       vnFull: 'Thiên Sơn Độn',        en: 'Retreat',                upperTriIdx: 0, lowerTriIdx: 6, description: 'Ẩn lui, rút lui. Biết lùi đúng thời.' },
  { num: 34, cn: '大壯', pinyin: 'dà zhuàng', vn: 'Đại Tráng', vnFull: 'Lôi Thiên Đại Tráng', en: 'Great Power',           upperTriIdx: 3, lowerTriIdx: 0, description: 'Mạnh lớn, hùng tráng. Sấm vang trên trời.' },
  { num: 35, cn: '晉', pinyin: 'jìn',      vn: 'Tấn',       vnFull: 'Hỏa Địa Tấn',          en: 'Progress',               upperTriIdx: 2, lowerTriIdx: 7, description: 'Tiến lên, thăng tiến. Mặt trời mọc trên đất.' },
  { num: 36, cn: '明夷', pinyin: 'míng yí', vn: 'Minh Di',   vnFull: 'Địa Hỏa Minh Di',      en: 'Darkening of Light',     upperTriIdx: 7, lowerTriIdx: 2, description: 'Sáng bị tổn thương. Mặt trời lặn dưới đất.' },
  { num: 37, cn: '家人', pinyin: 'jiā rén', vn: 'Gia Nhân',  vnFull: 'Phong Hỏa Gia Nhân',   en: 'The Family',             upperTriIdx: 4, lowerTriIdx: 2, description: 'Gia đình, gia đạo. Gió từ lửa, hơi ấm tỏa khắp nhà.' },
  { num: 38, cn: '睽', pinyin: 'kuí',      vn: 'Khuê',      vnFull: 'Hỏa Trạch Khuê',       en: 'Opposition',             upperTriIdx: 2, lowerTriIdx: 1, description: 'Trái ngược, bất hòa. Hai chị em ly tán.' },
  { num: 39, cn: '蹇', pinyin: 'jiǎn',     vn: 'Kiển',      vnFull: 'Thủy Sơn Kiển',        en: 'Obstruction',            upperTriIdx: 5, lowerTriIdx: 6, description: 'Gian nan, trở ngại. Nước trước núi sau, tiến thoái lưỡng nan.' },
  { num: 40, cn: '解', pinyin: 'xiè',      vn: 'Giải',      vnFull: 'Lôi Thủy Giải',        en: 'Deliverance',            upperTriIdx: 3, lowerTriIdx: 5, description: 'Giải thoát, tháo gỡ. Sấm và mưa, giải tỏa áp lực.' },
  { num: 41, cn: '損', pinyin: 'sǔn',      vn: 'Tổn',       vnFull: 'Sơn Trạch Tổn',        en: 'Decrease',               upperTriIdx: 6, lowerTriIdx: 1, description: 'Bớt đi, tổn giảm. Giảm dưới bổ trên.' },
  { num: 42, cn: '益', pinyin: 'yì',       vn: 'Ích',        vnFull: 'Phong Lôi Ích',         en: 'Increase',               upperTriIdx: 4, lowerTriIdx: 3, description: 'Thêm vào, gia tăng. Giảm trên bổ dưới.' },
  { num: 43, cn: '夬', pinyin: 'guài',     vn: 'Quải',      vnFull: 'Trạch Thiên Quải',      en: 'Breakthrough',           upperTriIdx: 1, lowerTriIdx: 0, description: 'Quyết đoán, cắt đứt. Năm dương trục một âm.' },
  { num: 44, cn: '姤', pinyin: 'gòu',      vn: 'Cấu',       vnFull: 'Thiên Phong Cấu',       en: 'Coming to Meet',         upperTriIdx: 0, lowerTriIdx: 4, description: 'Gặp gỡ bất ngờ. Một âm len vào năm dương.' },
  { num: 45, cn: '萃', pinyin: 'cuì',      vn: 'Tụy',       vnFull: 'Trạch Địa Tụy',        en: 'Gathering',              upperTriIdx: 1, lowerTriIdx: 7, description: 'Tụ họp, quần tụ. Đầm trên đất, mọi người quy tụ.' },
  { num: 46, cn: '升', pinyin: 'shēng',    vn: 'Thăng',     vnFull: 'Địa Phong Thăng',       en: 'Pushing Upward',         upperTriIdx: 7, lowerTriIdx: 4, description: 'Đi lên, thăng tiến. Cây mọc trong đất.' },
  { num: 47, cn: '困', pinyin: 'kùn',      vn: 'Khốn',      vnFull: 'Trạch Thủy Khốn',      en: 'Oppression',             upperTriIdx: 1, lowerTriIdx: 5, description: 'Khốn khó, bế tắc. Đầm cạn nước, kiệt quệ.' },
  { num: 48, cn: '井', pinyin: 'jǐng',     vn: 'Tỉnh',      vnFull: 'Thủy Phong Tỉnh',      en: 'The Well',               upperTriIdx: 5, lowerTriIdx: 4, description: 'Giếng nước, nguồn nuôi dưỡng. Bất biến giữa muôn biến.' },
  { num: 49, cn: '革', pinyin: 'gé',       vn: 'Cách',      vnFull: 'Trạch Hỏa Cách',       en: 'Revolution',             upperTriIdx: 1, lowerTriIdx: 2, description: 'Cải cách, thay đổi. Lửa trong đầm, đối nghịch tất biến.' },
  { num: 50, cn: '鼎', pinyin: 'dǐng',     vn: 'Đỉnh',      vnFull: 'Hỏa Phong Đỉnh',       en: 'The Cauldron',           upperTriIdx: 2, lowerTriIdx: 4, description: 'Cái đỉnh, chuyển hóa. Nấu nướng, luyện đan, văn hóa.' },
  { num: 51, cn: '震', pinyin: 'zhèn',     vn: 'Chấn',      vnFull: 'Thuần Chấn',            en: 'The Arousing',           upperTriIdx: 3, lowerTriIdx: 3, description: 'Sấm chồng sấm. Chấn động liên tiếp, tỉnh thức.' },
  { num: 52, cn: '艮', pinyin: 'gèn',      vn: 'Cấn',       vnFull: 'Thuần Cấn',             en: 'Keeping Still',          upperTriIdx: 6, lowerTriIdx: 6, description: 'Núi chồng núi. Dừng lại, tĩnh lặng, thiền định.' },
  { num: 53, cn: '漸', pinyin: 'jiàn',     vn: 'Tiệm',      vnFull: 'Phong Sơn Tiệm',       en: 'Gradual Progress',       upperTriIdx: 4, lowerTriIdx: 6, description: 'Tiến dần, từng bước. Cây trên núi, lớn chậm mà vững.' },
  { num: 54, cn: '歸妹', pinyin: 'guī mèi', vn: 'Quy Muội', vnFull: 'Lôi Trạch Quy Muội',   en: 'Marrying Maiden',        upperTriIdx: 3, lowerTriIdx: 1, description: 'Gả em gái. Sấm trên đầm, quan hệ không chính vị.' },
  { num: 55, cn: '豐', pinyin: 'fēng',     vn: 'Phong',     vnFull: 'Lôi Hỏa Phong',        en: 'Abundance',              upperTriIdx: 3, lowerTriIdx: 2, description: 'Phong phú, đầy đủ. Sấm và lửa, cực thịnh.' },
  { num: 56, cn: '旅', pinyin: 'lǚ',       vn: 'Lữ',        vnFull: 'Hỏa Sơn Lữ',           en: 'The Wanderer',           upperTriIdx: 2, lowerTriIdx: 6, description: 'Lữ hành, phiêu bạt. Lửa trên núi, không nơi dừng chân lâu.' },
  { num: 57, cn: '巽', pinyin: 'xùn',      vn: 'Tốn',       vnFull: 'Thuần Tốn',             en: 'The Gentle',             upperTriIdx: 4, lowerTriIdx: 4, description: 'Gió chồng gió. Nhẹ nhàng thấm nhuần, thuận theo.' },
  { num: 58, cn: '兌', pinyin: 'duì',      vn: 'Đoài',      vnFull: 'Thuần Đoài',            en: 'The Joyous',             upperTriIdx: 1, lowerTriIdx: 1, description: 'Đầm chồng đầm. Vui vẻ, hòa duyệt, khai mở.' },
  { num: 59, cn: '渙', pinyin: 'huàn',     vn: 'Hoán',      vnFull: 'Phong Thủy Hoán',       en: 'Dispersion',             upperTriIdx: 4, lowerTriIdx: 5, description: 'Tan rã, phân tán. Gió thổi trên nước, tan nhưng để tụ lại.' },
  { num: 60, cn: '節', pinyin: 'jié',      vn: 'Tiết',      vnFull: 'Thủy Trạch Tiết',      en: 'Limitation',             upperTriIdx: 5, lowerTriIdx: 1, description: 'Tiết chế, điều độ. Nước trong đầm, có mức có độ.' },
  { num: 61, cn: '中孚', pinyin: 'zhōng fú', vn: 'Trung Phu', vnFull: 'Phong Trạch Trung Phu', en: 'Inner Truth',           upperTriIdx: 4, lowerTriIdx: 1, description: 'Lòng thành, tín nhiệm. Gió trên đầm, chân thành cảm hóa.' },
  { num: 62, cn: '小過', pinyin: 'xiǎo guò', vn: 'Tiểu Quá', vnFull: 'Lôi Sơn Tiểu Quá',     en: 'Small Excess',           upperTriIdx: 3, lowerTriIdx: 6, description: 'Quá nhỏ, tiểu tiết. Chim nhỏ bay qua, nên giữ mức thấp.' },
  { num: 63, cn: '既濟', pinyin: 'jì jì',   vn: 'Ký Tế',    vnFull: 'Thủy Hỏa Ký Tế',       en: 'After Completion',       upperTriIdx: 5, lowerTriIdx: 2, description: 'Đã xong, hoàn thành. Âm dương đúng vị nhưng đã đỉnh.' },
  { num: 64, cn: '未濟', pinyin: 'wèi jì',  vn: 'Vị Tế',    vnFull: 'Hỏa Thủy Vị Tế',       en: 'Before Completion',      upperTriIdx: 2, lowerTriIdx: 5, description: 'Chưa xong, chưa hoàn thành. Âm dương lệch vị, vẫn đang tiến.' },
]
```

- [ ] **Step 3: Add ICHING_DATA and helper functions**

Open `hexagrams64.html` at line 1418. Copy the `ICHING_DATA` object (lines 1418–~1761) and convert to a typed export. The outer variable becomes:

```ts
export const ICHING_DATA: Record<number, IChingEntry> = {
  1: {
    judgment: {
      vn: "Càn: Nguyên, Hanh, Lợi, Trinh.",
      en: "The Creative works sublime success, furthering through perseverance."
    },
    image: {
      vn: "Trời vận hành mạnh mẽ. Người quân tử tự cường không ngừng.",
      en: "The movement of heaven is full of power. Thus the superior man makes himself strong and untiring."
    },
    lines: [
      { vn: "Rồng kiêu ngạo sẽ có điều hối hận.", en: "Arrogant dragon will have cause to repent." },
      { vn: "Rồng bay trên trời. Lợi gặp đại nhân.", en: "Flying dragon in the heavens. It furthers one to see the great man." },
      { vn: "Bay lượn trên vực sâu. Không lỗi.", en: "Wavering flight over the depths. No blame." },
      { vn: "Suốt ngày quân tử sáng tạo. Tối đến lòng vẫn lo nghĩ. Nguy nhưng không lỗi.", en: "All day long the superior man is creatively active. At nightfall his mind is still beset with cares. Danger. No blame." },
      { vn: "Rồng hiện ngoài đồng. Lợi gặp đại nhân.", en: "Dragon appearing in the field. It furthers one to see the great man." },
      { vn: "Rồng ẩn. Chưa nên hành động.", en: "Hidden dragon. Do not act." }
    ]
  },
  // ... copy remaining 63 entries from hexagrams64.html lines 1437–1761, same structure
}
```

Then add the helper functions at the end of the file:

```ts
export function getHexLines(upperIdx: number, lowerIdx: number): number[] {
  // Returns 6 lines top-to-bottom: upper trigram lines then lower trigram lines
  return [...TRIGRAMS[upperIdx].lines, ...TRIGRAMS[lowerIdx].lines]
}

export function getHexElement(upperIdx: number): string {
  return TRIGRAMS[upperIdx].el
}

export function buildGridMatrix(): (Hexagram | undefined)[][] {
  const matrix: (Hexagram | undefined)[][] = []
  for (let i = 0; i < 8; i++) {
    matrix[i] = []
    for (let j = 0; j < 8; j++) {
      matrix[i][j] = HEXAGRAMS.find(h => h.upperTriIdx === i && h.lowerTriIdx === j)
    }
  }
  return matrix
}
```

- [ ] **Step 4: Verify the file compiles**

```bash
cd /Users/hit/Sites/bttb
npx tsc --noEmit
```

Expected: no errors relating to `hexagrams-data.ts`

- [ ] **Step 5: Commit**

```bash
git add src/lib/hexagrams-data.ts
git commit -m "feat: add hexagrams data file with TRIGRAMS, HEXAGRAMS, ICHING_DATA"
```

---

## Task 2: Install NavigationMenu

**Files:**
- Install: `src/components/ui/navigation-menu.tsx` (via shadcn)

- [ ] **Step 1: Install the component**

```bash
cd /Users/hit/Sites/bttb
npx shadcn@latest add navigation-menu
```

Expected output: "✔ Done" with `src/components/ui/navigation-menu.tsx` created. If prompted to overwrite anything, choose yes.

- [ ] **Step 2: Verify the component exists**

```bash
ls src/components/ui/navigation-menu.tsx
```

Expected: file exists

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/navigation-menu.tsx
git commit -m "feat: add shadcn NavigationMenu component"
```

---

## Task 3: Create AppNav component

**Files:**
- Create: `src/components/app-nav.tsx`

- [ ] **Step 1: Create the file**

```tsx
// src/components/app-nav.tsx
'use client'

import Link from 'next/link'
import {
  NavigationMenu,
  NavigationMenuContent,
  NavigationMenuItem,
  NavigationMenuLink,
  NavigationMenuList,
  NavigationMenuTrigger,
  navigationMenuTriggerStyle,
} from '@/components/ui/navigation-menu'

export function AppNav() {
  return (
    <NavigationMenu>
      <NavigationMenuList>
        <NavigationMenuItem>
          <NavigationMenuLink asChild className={navigationMenuTriggerStyle()}>
            <Link href="/bazi">Bát Tự</Link>
          </NavigationMenuLink>
        </NavigationMenuItem>
        <NavigationMenuItem>
          <NavigationMenuLink asChild className={navigationMenuTriggerStyle()}>
            <Link href="/bazi/cases">Cases</Link>
          </NavigationMenuLink>
        </NavigationMenuItem>
        <NavigationMenuItem>
          <NavigationMenuLink asChild className={navigationMenuTriggerStyle()}>
            <Link href="/readings">Lá Số</Link>
          </NavigationMenuLink>
        </NavigationMenuItem>
        <NavigationMenuItem>
          <NavigationMenuTrigger>Học</NavigationMenuTrigger>
          <NavigationMenuContent>
            <ul className="w-48 p-2">
              <li>
                <NavigationMenuLink asChild>
                  <Link
                    href="/hexagrams"
                    className="block rounded-md px-3 py-2 text-sm leading-none no-underline outline-none transition-colors hover:bg-accent hover:text-accent-foreground focus:bg-accent focus:text-accent-foreground"
                  >
                    <div className="font-medium mb-1">64 Quẻ</div>
                    <p className="text-xs text-muted-foreground line-clamp-2">
                      Lục Thập Tứ Quái · I Ching reference
                    </p>
                  </Link>
                </NavigationMenuLink>
              </li>
            </ul>
          </NavigationMenuContent>
        </NavigationMenuItem>
      </NavigationMenuList>
    </NavigationMenu>
  )
}
```

> **Note:** If `navigationMenuTriggerStyle` is not exported by the installed component, replace `className={navigationMenuTriggerStyle()}` with `className="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors px-3 py-2"`. Check the generated `navigation-menu.tsx` exports.

- [ ] **Step 2: Verify it compiles**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add src/components/app-nav.tsx
git commit -m "feat: add AppNav component with Học dropdown"
```

---

## Task 4: Update layout.tsx

**Files:**
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Replace `<nav>` with `<AppNav />`**

Open `src/app/layout.tsx`. Replace the entire file with:

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SessionProvider } from "@/components/auth/session-provider";
import { UserButton } from "@/components/auth/user-button";
import { AppNav } from "@/components/app-nav";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BBTB App",
  description: "Next.js + Auth.js + Prisma starter",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <SessionProvider>
          <header className="flex items-center justify-between border-b px-6 py-3">
            <div className="flex items-center gap-6">
              <a href="/" className="text-lg font-semibold">BBTB</a>
              <div className="hidden sm:flex">
                <AppNav />
              </div>
            </div>
            <UserButton />
          </header>
          <main>{children}</main>
        </SessionProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Start dev server and verify topbar visually**

```bash
npm run dev
```

Open http://localhost:3000. Verify:
- "Bát Tự", "Cases", "Lá Số" links appear in the topbar
- Hovering "Học" shows a dropdown with "64 Quẻ"
- Clicking the existing links still navigates correctly

- [ ] **Step 3: Commit**

```bash
git add src/app/layout.tsx
git commit -m "feat: replace topbar nav with NavigationMenu, add Học dropdown"
```

---

## Task 5: Create hexagrams page

**Files:**
- Create: `src/app/hexagrams/page.tsx`

- [ ] **Step 1: Create the page file**

```tsx
// src/app/hexagrams/page.tsx
'use client'

import { useState, useMemo } from 'react'
import { Grid2x2, Circle } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader } from '@/components/ui/card'
import {
  TRIGRAMS,
  HEXAGRAMS,
  ICHING_DATA,
  ELEMENT_COLORS,
  getHexLines,
  getHexElement,
  buildGridMatrix,
  type Hexagram,
} from '@/lib/hexagrams-data'

// ---- Hex line renderer ----
function HexLines({
  upperIdx,
  lowerIdx,
  size = 'sm',
}: {
  upperIdx: number
  lowerIdx: number
  size?: 'sm' | 'lg'
}) {
  const lines = getHexLines(upperIdx, lowerIdx)
  const el = getHexElement(upperIdx)
  const color = ELEMENT_COLORS[el]
  const w = size === 'sm' ? 22 : 32
  const h = size === 'sm' ? 2 : 3
  const gap = size === 'sm' ? 4 : 6

  return (
    <div className="flex flex-col gap-[3px] items-center">
      {lines.map((isYang, i) =>
        isYang ? (
          <div
            key={i}
            style={{ width: w, height: h, background: color, borderRadius: h / 2 }}
          />
        ) : (
          <div key={i} style={{ width: w, display: 'flex', gap: gap }}>
            <div style={{ flex: 1, height: h, background: color, borderRadius: h / 2 }} />
            <div style={{ flex: 1, height: h, background: color, borderRadius: h / 2 }} />
          </div>
        )
      )}
    </div>
  )
}

// ---- Detail panel ----
function HexDetail({
  hex,
  onClose,
}: {
  hex: Hexagram
  onClose: () => void
}) {
  const el = getHexElement(hex.upperTriIdx)
  const color = ELEMENT_COLORS[el]
  const upper = TRIGRAMS[hex.upperTriIdx]
  const lower = TRIGRAMS[hex.lowerTriIdx]
  const iching = ICHING_DATA[hex.num]

  return (
    <Card className="mt-4">
      <CardHeader className="pb-3">
        <div className="flex items-start justify-between">
          <div className="flex items-center gap-4">
            <div className="text-4xl font-serif" style={{ color }}>
              {hex.cn}
            </div>
            <div>
              <div className="text-sm font-medium text-muted-foreground">{hex.num}. {hex.pinyin}</div>
              <div className="text-lg font-semibold">{hex.vnFull}</div>
              <div className="text-sm text-muted-foreground italic">{hex.en}</div>
            </div>
          </div>
          <Button variant="ghost" size="icon" onClick={onClose} className="shrink-0">
            ✕
          </Button>
        </div>
      </CardHeader>
      <CardContent className="space-y-4">
        {/* Trigrams */}
        <div className="flex justify-center gap-8">
          <div className="text-center">
            <div className="text-xs text-muted-foreground mb-1">Ngoại quái</div>
            <HexLines upperIdx={hex.upperTriIdx} lowerIdx={hex.upperTriIdx} size="lg" />
            <div className="mt-1 font-semibold text-sm">{upper.cn} {upper.vn}</div>
            <div className="text-xs text-muted-foreground">{upper.nature}</div>
          </div>
          <div className="text-center">
            <div className="text-xs text-muted-foreground mb-1">Nội quái</div>
            <HexLines upperIdx={hex.lowerTriIdx} lowerIdx={hex.lowerTriIdx} size="lg" />
            <div className="mt-1 font-semibold text-sm">{lower.cn} {lower.vn}</div>
            <div className="text-xs text-muted-foreground">{lower.nature}</div>
          </div>
        </div>

        {/* Description */}
        <p className="text-sm text-muted-foreground italic">{hex.description}</p>

        {/* Judgment */}
        {iching && (
          <div className="space-y-2 border-t pt-3">
            <div className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">
              Quái Từ · Judgment
            </div>
            <p className="text-sm">{iching.judgment.vn}</p>
            <p className="text-xs text-muted-foreground italic">{iching.judgment.en}</p>
          </div>
        )}

        {/* Image */}
        {iching && (
          <div className="space-y-2 border-t pt-3">
            <div className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">
              Tượng Từ · Image
            </div>
            <p className="text-sm">{iching.image.vn}</p>
            <p className="text-xs text-muted-foreground italic">{iching.image.en}</p>
          </div>
        )}
      </CardContent>
    </Card>
  )
}

// ---- Grid view (8×8 matrix) ----
function GridView({
  selectedTrigramIdx,
  onSelectHex,
}: {
  selectedTrigramIdx: number | null
  onSelectHex: (hex: Hexagram) => void
}) {
  const matrix = useMemo(() => buildGridMatrix(), [])

  function isHighlighted(hex: Hexagram) {
    if (selectedTrigramIdx === null) return true
    return hex.upperTriIdx === selectedTrigramIdx || hex.lowerTriIdx === selectedTrigramIdx
  }

  return (
    <div className="overflow-x-auto">
      <div className="grid grid-cols-9 gap-0.5 min-w-[400px]">
        {/* Header row */}
        <div className="flex items-end justify-end p-1">
          <span className="text-[10px] text-muted-foreground leading-tight text-right">
            Ngoại↓<br />Nội→
          </span>
        </div>
        {TRIGRAMS.map((t, j) => (
          <div key={j} className="flex flex-col items-center justify-end p-1 pb-2">
            <span className="text-sm font-bold" style={{ color: ELEMENT_COLORS[t.el] }}>{t.cn}</span>
            <span className="text-[10px] text-muted-foreground">{t.vn}</span>
          </div>
        ))}

        {/* Data rows */}
        {matrix.map((row, i) => (
          <>
            {/* Row header */}
            <div key={`rh-${i}`} className="flex flex-col items-center justify-center p-1">
              <span className="text-sm font-bold" style={{ color: ELEMENT_COLORS[TRIGRAMS[i].el] }}>{TRIGRAMS[i].cn}</span>
              <span className="text-[10px] text-muted-foreground">{TRIGRAMS[i].vn}</span>
            </div>
            {/* Cells */}
            {row.map((hex, j) =>
              hex ? (
                <button
                  key={`cell-${i}-${j}`}
                  onClick={() => onSelectHex(hex)}
                  className={`flex flex-col items-center justify-center gap-0.5 p-1.5 rounded border text-center transition-all min-h-[72px] ${
                    isHighlighted(hex)
                      ? 'hover:bg-muted/70 border-border cursor-pointer'
                      : 'opacity-25 border-transparent cursor-pointer'
                  }`}
                >
                  <span className="text-[10px] text-muted-foreground leading-none">{hex.num}</span>
                  <HexLines upperIdx={hex.upperTriIdx} lowerIdx={hex.lowerTriIdx} />
                  <span
                    className="text-sm font-bold leading-none"
                    style={{ color: ELEMENT_COLORS[getHexElement(hex.upperTriIdx)] }}
                  >
                    {hex.cn}
                  </span>
                  <span className="text-[10px] text-muted-foreground leading-none">{hex.vn}</span>
                </button>
              ) : (
                <div key={`empty-${i}-${j}`} />
              )
            )}
          </>
        ))}
      </div>
    </div>
  )
}

// ---- Circle view (SVG) ----
function CircleView({ onSelectHex }: { onSelectHex: (hex: Hexagram) => void }) {
  const size = 600
  const cx = size / 2
  const cy = size / 2
  const r1 = 90
  const r2 = 220
  const angleStep = 360 / 64
  const startAngle = -90

  return (
    <div className="flex justify-center">
      <svg
        viewBox={`0 0 ${size} ${size}`}
        className="w-full max-w-xl"
        xmlns="http://www.w3.org/2000/svg"
      >
        {/* Guide circles */}
        <circle cx={cx} cy={cy} r={r2 + 15} fill="none" stroke="currentColor" strokeOpacity="0.08" strokeWidth="0.8" />
        <circle cx={cx} cy={cy} r={r2} fill="none" stroke="currentColor" strokeOpacity="0.08" strokeWidth="0.8" />
        <circle cx={cx} cy={cy} r={r1} fill="none" stroke="currentColor" strokeOpacity="0.08" strokeWidth="0.8" />

        {/* Center symbol */}
        <text
          x={cx} y={cy}
          textAnchor="middle" dominantBaseline="middle"
          fontSize="28" opacity="0.2"
          className="fill-foreground"
        >
          ☯
        </text>

        {/* 64 hexagram wedges */}
        {HEXAGRAMS.map((hex, idx) => {
          const angle = startAngle + idx * angleStep
          const rad = angle * Math.PI / 180
          const a1 = (angle - angleStep / 2) * Math.PI / 180
          const a2 = (angle + angleStep / 2) * Math.PI / 180
          const el = getHexElement(hex.upperTriIdx)
          const color = ELEMENT_COLORS[el]
          const midR = (r1 + r2) / 2

          const x1o = cx + r2 * Math.cos(a1), y1o = cy + r2 * Math.sin(a1)
          const x2o = cx + r2 * Math.cos(a2), y2o = cy + r2 * Math.sin(a2)
          const x1i = cx + r1 * Math.cos(a1), y1i = cy + r1 * Math.sin(a1)
          const x2i = cx + r1 * Math.cos(a2), y2i = cy + r1 * Math.sin(a2)

          const labelR = midR + 12
          const lx = cx + labelR * Math.cos(rad)
          const ly = cy + labelR * Math.sin(rad)
          const rotation = (angle > 90 || angle < -90) ? angle + 180 : angle

          return (
            <g key={idx} onClick={() => onSelectHex(hex)} style={{ cursor: 'pointer' }}>
              <path
                d={`M${x1o},${y1o} A${r2},${r2} 0 0,1 ${x2o},${y2o} L${x2i},${y2i} A${r1},${r1} 0 0,0 ${x1i},${y1i} Z`}
                fill={color}
                stroke="white"
                strokeWidth="0.5"
                opacity="0.18"
                className="hover:opacity-50 transition-opacity"
              />
              <text
                x={lx} y={ly}
                textAnchor="middle" dominantBaseline="middle"
                fontSize="8"
                fill={color}
                fontWeight="700"
                transform={`rotate(${rotation},${lx},${ly})`}
              >
                {hex.cn}
              </text>
            </g>
          )
        })}
      </svg>
    </div>
  )
}

// ---- Main page ----
export default function HexagramsPage() {
  const [view, setView] = useState<'grid' | 'circle'>('grid')
  const [selectedTrigramIdx, setSelectedTrigramIdx] = useState<number | null>(null)
  const [selectedHex, setSelectedHex] = useState<Hexagram | null>(null)

  function handleSelectTrigram(idx: number | null) {
    setSelectedTrigramIdx(prev => (prev === idx ? null : idx))
    setSelectedHex(null)
  }

  function handleSelectHex(hex: Hexagram) {
    setSelectedHex(prev => (prev?.num === hex.num ? null : hex))
  }

  return (
    <div className="container mx-auto max-w-5xl px-4 py-8">
      {/* Header */}
      <div className="mb-6">
        <h1 className="text-3xl font-bold">64 Quẻ Kinh Dịch</h1>
        <p className="text-muted-foreground mt-1">Lục Thập Tứ Quái · 64 Hexagrams of the I Ching</p>
      </div>

      {/* Controls */}
      <div className="flex flex-wrap items-center gap-2 mb-4">
        {/* View toggle */}
        <div className="flex rounded-md border overflow-hidden mr-2">
          <button
            onClick={() => setView('grid')}
            className={`flex items-center gap-1.5 px-3 py-1.5 text-sm transition-colors ${
              view === 'grid' ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
            }`}
          >
            <Grid2x2 className="w-3.5 h-3.5" />
            Ma trận
          </button>
          <button
            onClick={() => setView('circle')}
            className={`flex items-center gap-1.5 px-3 py-1.5 text-sm transition-colors ${
              view === 'circle' ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
            }`}
          >
            <Circle className="w-3.5 h-3.5" />
            Vòng tròn
          </button>
        </div>

        {/* Trigram filter (grid view only) */}
        {view === 'grid' && (
          <>
            <Button
              variant={selectedTrigramIdx === null ? 'default' : 'outline'}
              size="sm"
              onClick={() => handleSelectTrigram(null)}
            >
              Tất Cả
            </Button>
            {TRIGRAMS.map((t, i) => (
              <Button
                key={i}
                variant={selectedTrigramIdx === i ? 'default' : 'outline'}
                size="sm"
                onClick={() => handleSelectTrigram(i)}
                style={selectedTrigramIdx === i ? {} : { color: ELEMENT_COLORS[t.el] }}
              >
                {t.cn} {t.vn}
              </Button>
            ))}
          </>
        )}
      </div>

      {/* View */}
      {view === 'grid' ? (
        <GridView
          selectedTrigramIdx={selectedTrigramIdx}
          onSelectHex={handleSelectHex}
        />
      ) : (
        <CircleView onSelectHex={handleSelectHex} />
      )}

      {/* Detail panel */}
      {selectedHex && (
        <HexDetail
          hex={selectedHex}
          onClose={() => setSelectedHex(null)}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 2: Start dev server and verify the page**

```bash
npm run dev
```

Open http://localhost:3000/hexagrams. Verify:
- Page title "64 Quẻ Kinh Dịch" appears
- Grid view shows 8×8 matrix with trigram row/column headers
- Each cell shows hexagram number, line bars, Chinese character, Vietnamese name
- Clicking a trigram filter dims non-matching cells
- Clicking a cell opens the detail panel below with name, trigrams, description, judgment
- View toggle switches to circle view
- Circle view shows 64 wedges arranged in a ring
- Clicking a wedge in circle view opens the detail panel

- [ ] **Step 3: Check "64 Quẻ" link in topbar**

On any page (e.g., http://localhost:3000), hover "Học" in the topbar. Click "64 Quẻ". Verify it navigates to `/hexagrams`.

- [ ] **Step 4: Commit**

```bash
git add src/app/hexagrams/page.tsx
git commit -m "feat: add 64 Quẻ hexagrams learning page at /hexagrams"
```

---

## Self-Review

**Spec coverage check:**
- ✅ NavigationMenu replaces `<nav>` (Task 3 + 4)
- ✅ "Học" dropdown with "64 Quẻ" → `/hexagrams` (Task 3)
- ✅ Existing links (Bát Tự, Cases, Lá Số) preserved (Task 3)
- ✅ Data extracted to `hexagrams-data.ts` (Task 1)
- ✅ Grid view 8×8 matrix (Task 5)
- ✅ Circle view SVG (Task 5)
- ✅ Trigram filter tabs (Task 5)
- ✅ Detail panel inline below grid (Task 5) — shows num, cn/vn, trigrams, description, judgment
- ✅ App light theme used throughout (no dark-gold overrides)
- ✅ Public page — no auth guard added

**Type consistency check:**
- `getHexLines(upperIdx, lowerIdx)` — used same signature in helpers and page ✅
- `getHexElement(upperIdx)` — used same signature in helpers and page ✅
- `buildGridMatrix()` — returns `(Hexagram | undefined)[][]`, handled in GridView ✅
- `ICHING_DATA[hex.num]` — keyed by `hex.num` (number 1–64), matches `Record<number, IChingEntry>` ✅

**Note:** No test framework is configured in this project. Manual verification steps are included in each task.
